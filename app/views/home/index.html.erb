<header>
  <h1>
    <strong>M</strong>inistry
    <span><small>of</small></span>
    <strong>V</strong>elocity
  </h1>
</header>

<div class="row-fluid">
  <div class="span10 offset1 mobile-anchor">
    <div class="row-fluid">
      <% t(:rationale, scope: :home).each do |section_id, section| %>
      <section class="span4 <%= section_id %>">
        <h3><%= section[:title] %></h3>
        <article>
          <p><%= section[:text] %></p>
        </article>
      </section>
      <% end %>
    </div>
  </div>
</div>
